# Hello World in CYPHER
# Bonjour le monde en CYPHER
# 
# This is the simplest possible CYPHER program
# C'est le programme CYPHER le plus simple possible
#
# Run: cypher run examples/hello_world.cypher
# Exécuter: cypher run examples/hello_world.cypher

# Create a button with text / Créer un bouton avec texte
A(hello_button);G("Hello, CYPHER!");T(primary):

# Fix state and launch / Fixer état et lancer
F(fixed):
L(life):

# Expected output / Sortie attendue:
# <button id="hello_button" class="primary">Hello, CYPHER!</button>